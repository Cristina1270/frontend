services:
  frontend:
    image: frontend:v1.0.0
    container_name: frontend
    ports:
      - "80:80"
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge